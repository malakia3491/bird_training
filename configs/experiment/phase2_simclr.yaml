# @package _global_

defaults:
  - override /task: ssl_contrastive
  - override /model: panns
  - override /data: ssl

experiment_name: "Phase2_SSL"
run_name: "PANNs_SimCLR_Strong"

# Специфичные настройки
trainer:
  max_epochs: 100
  accumulate_grad_batches: 4

optimizer:
  lr: 1e-4
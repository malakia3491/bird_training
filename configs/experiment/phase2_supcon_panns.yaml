# @package _global_

defaults:
  - override /task: supcon
  - override /model: panns
  - override /data: ssl
  - override /augmentation: contrastive_strong 

experiment_name: "Phase2_SupCon"
run_name: "PANNs_StrongAugs"

data:
  train_transform: ${augmentation} 
  m_per_class: 4 

optimizer:
  lr: 1e-5

trainer:
  max_epochs: 100
  accumulate_grad_batches: 4
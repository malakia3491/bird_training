_target_: src.data_loaders.bird_datamodule.BirdDataModule # Hydra instantiation target

root_dir: "D:/coding/data/birds_common/data_russian" # Или путь, где лежит папка mel, splits, manifest
batch_size: 64 # Для спектрограмм можно брать батч побольше, чем для аудио
num_workers: 2
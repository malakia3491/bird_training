# 🦜 Bird Audio Detection & Classification Framework

Этот проект представляет собой модульную систему для обучения нейросетей распознаванию звуков птиц. Проект построен на стеке **PyTorch Lightning + Hydra + Timm** и поддерживает полный цикл экспериментов: от выбора входных признаков (LogMel/PCEN) до Self-Supervised Learning (SSL) и Sound Event Detection (SED).

## 📋 Оглавление
1. [Установка и Окружение](#1-установка-и-окружение)
2. [Структура Проекта](#2-структура-проекта)
3. [Подготовка Данных](#3-подготовка-данных)
4. [Запуск Обучения и Эксперименты](#4-запуск-обучения-и-эксперименты)
5. [Конфигурация (Hydra)](#5-конфигурация-hydra)
6. [План Развития (Roadmap)](#6-план-развития-roadmap)

---

## 1. Установка и Окружение

Проект использует менеджер пакетов `uv` для строгой фиксации зависимостей и корректной работы с CUDA.

### Предварительные требования
*   Python 3.10+
*   NVIDIA Driver (поддерживающий CUDA 12.4+)
*   Установленный `uv` (если нет: `pip install uv`)

### Установка с нуля

```bash
# 1. Клонирование репозитория (или создание папки)
mkdir bird_training
cd bird_training

# 2. Инициализация окружения и установка зависимостей
# (uv сам скачает нужный Python и PyTorch под CUDA)
uv sync
```

### Проверка GPU
Чтобы убедиться, что PyTorch видит видеокарту:
```bash
uv run python -c "import torch; print(torch.cuda.is_available())"
# Должно вывести: True
```

---

## 2. Структура Проекта

Проект следует паттерну "Модульный монолит". Все настройки вынесены в конфиги, код разбит на логические модули.

```text
bird_training/
├── configs/                     # ⚙️ ЦЕНТР УПРАВЛЕНИЯ (Hydra)
│   ├── train_config.yaml        # Главный конфиг обучения
│   ├── data/                    # Настройки путей к данным
│   │   └── real.yaml            # Пути к реальному датасету
│   ├── model/                   # Архитектуры (EfficientNet, AST, ResNet)
│   │   └── efficientnet.yaml
│   └── frontend/                # Обработка звука (LogMel, PCEN)
│       ├── logmel.yaml
│       └── pcen.yaml
│
├── src/                         # 🧠 ИСХОДНЫЙ КОД
│   ├── data.py                  # DataModule (загрузка и батчинг данных)
│   ├── system.py                # LightningModule (логика обучения, loss, optimizer)
│   ├── modules/                 # "Кирпичики" нейросети
│   │   ├── backbone.py          # Энкодеры (timm)
│   │   └── frontend.py          # Слои предобработки (Mel, PCEN) на GPU
│   └── utils/
│       └── reporter.py          # Генерация отчета REPORT.md
│
├── outputs/                     # 📂 РЕЗУЛЬТАТЫ
│   └── YYYY-MM-DD/              # Папки экспериментов (создаются автоматически)
│       └── HH-MM-SS/
│           ├── checkpoints/     # Веса модели (.ckpt)
│           ├── REPORT.md        # Итоговый отчет с метриками
│           └── .hydra/          # Конфиг, с которым запускали
│
├── train.py                     # 🚀 Точка входа для обучения
├── pyproject.toml               # Зависимости проекта
└── README.md                    # Этот файл
```

---

## 3. Подготовка Данных

Проект ожидает данные в специфической структуре (собранной скриптами `process_birdclef`, `process_dcase` и т.д.).

**Корневая папка** (указывается в `configs/data/real.yaml`) должна содержать:

*   **`type0_negative_examples/`**: Аудио без птиц (шум, дождь, машины).
    *   `audio/` (файлы .wav)
    *   `metadata.csv` (столбцы: `filename`, `label`='negative')
*   **`type3_weak_specific/`**: Записи с известным видом птицы, но без времени (Weak Labels).
    *   `audio/`
    *   `metadata.csv` (столбцы: `filename`, `primary_label`)
*   **`type4_strong_specific/`**: Записи с известным видом и таймстампами.
    *   `audio/`
    *   `metadata.csv` (столбцы: `filename`, `primary_label`, `start_time`, `end_time`)

*Остальные папки (`type1`, `type2`) пока не используются в базовом обучении.*

**Настройка:**
Откройте `configs/data/real.yaml` и укажите путь:
```yaml
root_dir: "D:/path/to/FINAL_DATASET"
```

---

## 4. Запуск Обучения и Эксперименты

Благодаря Hydra, вам не нужно менять код, чтобы запустить новый эксперимент. Вы меняете параметры прямо в командной строке.

### Базовый запуск (Baseline)
Запуск с настройками по умолчанию (EfficientNet-B0 + LogMel):
```bash
uv run train.py
```

### Эксперимент 1: Сравнение входных данных
Проверяем, работает ли PCEN лучше LogMel:

1.  **Запуск на LogMel:**
    ```bash
    uv run train.py frontend=logmel project_name="Exp1_LogMel"
    ```
2.  **Запуск на PCEN:**
    ```bash
    uv run train.py frontend=pcen project_name="Exp1_PCEN"
    ```

### Эксперимент 2: Смена архитектуры
Хотим попробовать ResNet вместо EfficientNet:
```bash
uv run train.py model=resnet50
```

### Изменение параметров обучения
```bash
# Изменить размер батча и кол-во эпох
uv run train.py data.batch_size=16 trainer.max_epochs=20

# Изменить learning rate
uv run train.py optimizer.lr=0.0005
```

---

## 5. Конфигурация (Hydra)

Как это работает? В `configs/train_config.yaml` есть блок `defaults`:

```yaml
defaults:
  - model: efficientnet  # Берет настройки из configs/model/efficientnet.yaml
  - frontend: logmel     # Берет настройки из configs/frontend/logmel.yaml
  - data: real           # Берет настройки из configs/data/real.yaml
```

Чтобы создать новую конфигурацию (например, новую модель):
1.  Создайте файл `configs/model/mymodel.yaml`.
2.  Запускайте: `uv run train.py model=mymodel`.

---
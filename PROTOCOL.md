# 🧪 Протокол Экспериментов (SOP)

Этот документ регламентирует порядок проведения экспериментов для обеспечения воспроизводимости и чистоты результатов.

---

## 1. Золотые Правила (Golden Rules)

1.  **Никогда не меняй дефолтные конфиги** (`configs/model/`, `configs/data/`) для конкретного эксперимента.
    *   ❌ *Плохо:* Поменять `lr` в `configs/optimizer/adamw.yaml`.
    *   ✅ *Хорошо:* Создать файл в `configs/experiment/my_run.yaml` и переопределить `lr` там.
2.  **Один Запуск = Один Конфиг Эксперимента.**
    *   Каждая гипотеза должна иметь свой файл в `configs/experiment/`. Название файла должно быть говорящим.
3.  **Следи за LabelEncoder.**
    *   Если меняешь датасет (добавляешь данные) — **УДАЛЯЙ** `checkpoints/label_encoder.pkl`. Он должен пересоздаться.
4.  **Проверяй REPORT.md.**
    *   После обучения всегда открывай отчет. Если там `NaN` или нули в метриках — эксперимент не засчитан, ищи баг.

---

## 2. Именование (Naming Convention)

При создании файла в `configs/experiment/` и запуске используй следующую схему:

`Phase[N]_[Method]_[Model]_[Details]`

*   **Phase1:** Baselines (Supervised обучение с нуля или ImageNet).
*   **Phase2:** Representation Learning (SSL: SimCLR, SupCon, DAE).
*   **Phase3:** Fine-Tuning / Metric Learning (Применение весов из Phase2).

**Примеры:**
*   `Phase1_Supervised_PANNs`
*   `Phase2_SupCon_EfficientNet_StrongAugs`
*   `Phase3_Metric_ArcFace_from_SupCon`

---

## 3. Как создать новый эксперимент

### Шаг 1. Создать файл конфига
Создайте файл `configs/experiment/phase2_supcon_test.yaml`:

```yaml
# @package _global_

# 1. Наследуемся от задачи и компонентов
defaults:
  - override /task: supcon
  - override /model: panns
  - override /data: labeled  # SupCon требует метки!

# 2. Имена для папок (ОБЯЗАТЕЛЬНО)
experiment_name: "Phase2_SupCon"
run_name: "PANNs_Run1"

# 3. Переопределение параметров
trainer:
  max_epochs: 50

optimizer:
  lr: 1e-4

# Для SupCon важен размер батча и сэмплер
data:
  batch_size: 64
  m_per_class: 4  # 4 примера на класс в батче
```

### Шаг 2. Запустить
```bash
python main.py experiment=phase2_supcon_test
```

---

## 4. Пайплайн обучения (Workflow)

### Сценарий А: Обучение классификатора (Baseline)
1.  Создать конфиг эксперимента с `task: classification`.
2.  Запустить.
3.  Проверить F1 в `REPORT.md`.

### Сценарий Б: Двухэтапное обучение (SSL -> Fine-Tuning)

**Этап 1: Pre-training**
1.  Создать конфиг `experiment/phase2_supcon.yaml` (`task: supcon`).
2.  Запустить обучение.
3.  Найти файл чекпоинта: `outputs/Phase2_SupCon/.../checkpoints/last.ckpt`.

**Этап 2: Fine-Tuning**
1.  Создать конфиг `experiment/phase3_finetune.yaml` (`task: classification` или `metric_learning`).
2.  В конфиге указать путь к весам:
    ```yaml
    pretrained_from: "outputs/Phase2_SupCon/.../checkpoints/last.ckpt"
    ```
3.  Использовать низкий LR (`1e-4`).
4.  Запустить и сравнить результат с Baseline.

---

## 5. Чек-лист перед коммитом / сдачей

- [ ] Эксперимент запускается через `python main.py experiment=...`.
- [ ] В папке `outputs` создался корректный отчет.
- [ ] Графики Loss падают (не прямая линия, не шум).
- [ ] Если менялись данные — `label_encoder.pkl` был обновлен.


## 6. Работа с Данными (Data Management)

В проекте два вида данных:
1.  **Raw Audio (`.ogg`, `.mp3`)** — лежат в архивах или папках. Не используются напрямую при обучении.
2.  **Precomputed MelSpec (`.npy`)** — готовые тензоры для обучения. Лежат в папке, указанной в `configs/data/*.yaml`.

### Правила добавления данных
1.  **Никогда не смешивай** старые и новые данные в одной папке вручную. Используй скрипты.
2.  Если пришли новые аудиофайлы:
    *   Используй `python scripts/data/prepare_ssl_data_csv.py`.
    *   Укажи `--output_dir` в **новую** папку (например, `processed_v2`).
    *   Измени путь в конфиге эксперимента.
3.  **LabelEncoder Reset:**
    *   При смене папки с данными или добавлении классов **ОБЯЗАТЕЛЬНО** удали файл `checkpoints/label_encoder.pkl` в папке с данными.
    *   Если этого не сделать, словите ошибку `ValueError: y contains previously unseen labels`.

---

## 7. Анализ Результатов

Обучение — это только половина дела. Важно правильно интерпретировать цифры.

### Инструменты
1.  **`REPORT.md`**: Генерируется автоматически в папке эксперимента.
    *   Смотреть: Графики Loss и F1.
    *   *Красный флаг:* Если Train Loss падает, а Val Loss растет — переобучение. Включайте аугментации или Mixup.
2.  **`metrics.csv`**: Лежит в папке `logs/`. Содержит сырые данные по шагам.
    *   Визуализация: `python scripts/utils/plot_csv.py "path/to/metrics.csv"`
3.  **`test_all_splits.py`**:
    *   Используйте этот скрипт для финальной проверки модели на Train/Val/Test выборках.
    *   *Важно:* Убедитесь, что `label_encoder.pkl` соответствует тому, на котором модель училась.

### Критерии успеха (Success Metrics)
| Задача | Метрика | Хороший результат | Примечание |
| :--- | :--- | :--- | :--- |
| **Classification** | Macro F1 | > 0.85 | Зависит от сложности классов |
| **SimCLR (SSL)** | NT-Xent Loss | 1.5 - 2.5 | Не должен падать в 0.01! |
| **SupCon** | SupCon Loss | 0.5 - 1.5 | Должен быть ниже, чем SimCLR |
| **Metric (ArcFace)** | Macro F1 | > 0.88 | Должен быть выше Baseline на редких классах |

---

## 8. Устранение частых проблем (Troubleshooting)

### Q: Ошибка `ConfigAttributeError: Key ... is not in struct`
**Причина:** Ошибка в структуре YAML файла (отступы, пропущенные двоеточия) или попытка обратиться к несуществующему полю.
**Решение:** Проверьте синтаксис конфига. Используйте `defaults` для подключения внешних файлов.

### Q: Ошибка `ValueError: y contains previously unseen labels`
**Причина:** В датасете есть классы, которых нет в `label_encoder.pkl`.
**Решение:** Удалить `label_encoder.pkl`.

### Q: Ошибка `CUDA out of memory`
**Причина:** Батч не влезает в видеопамять.
**Решение:**
1.  Уменьшить `batch_size` в конфиге эксперимента.
2.  Увеличить `accumulate_grad_batches` в конфиге трейнера (чтобы сохранить эффективный размер батча).

### Q: Ошибка при загрузке чекпоинта `Weights only load failed`
**Причина:** Защита PyTorch 2.6+.
**Решение:** Загружать веса вручную через `torch.load(..., weights_only=False)`.

---

## 9. Финальная структура репозитория

```text
project/
├── configs/               # Все настройки (Гиперпараметры)
│   ├── experiment/        # <-- СЮДА ДОБАВЛЯТЬ НОВЫЕ ГИПОТЕЗЫ
│   ├── task/              # Шаблоны задач (SupCon, Classif...)
│   └── ...
├── src/                   # Исходный код (Не менять без нужды)
│   ├── systems/           # Логика обучения (LightningModules)
│   ├── data_loaders/      # Аугментации и Сэмплеры
│   └── modules/           # Нейросети
├── scripts/               # Утилиты (подготовка данных, графики)
├── outputs/               # Результаты (НЕ КОММИТИТЬ В GIT)
├── main.py                # Точка входа
└── PROTOCOL.md            # Этот файл
```

---

### Чек-лист для коммита ("Я сделал фичу, что дальше?")

Если вы добавили новую крутую аугментацию или модель:
1.  Код должен лежать в `src/`.
2.  Должен быть создан новый конфиг в `configs/model/` или `configs/augmentation/`.
3.  **Не ломайте** старые конфиги (`defaults`). Лучше создайте новый файл.
4.  Проверьте, что `python main.py experiment=...` запускается на чистой машине.